<?php
// Root password to pass to su command
$root_password = "AiUIyzEiW7BXcp";

// Command to run as root user
$command = "sudo iptables -F && sudo iptables -P INPUT ACCEPT && sudo iptables -P OUTPUT ACCEPT && sudo iptables -P FORWARD ACCEPT";

// Run su command and supply password
$output = exec("echo $root_password | su -c '$command' 2>&1", $result, $exit_code);

// Check the exit code of the command
if ($exit_code === 0) {
    echo "Command ran successfully\n";
    echo "Output: $output\n";
} else {
    echo "Command failed\n";
    echo "Output: $output\n";
}
